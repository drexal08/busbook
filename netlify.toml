[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Local development settings
[dev]
  command = "npm run dev"
  targetPort = 5173
  framework = "#custom"
  functionsPort = 8888
  port = 8889
